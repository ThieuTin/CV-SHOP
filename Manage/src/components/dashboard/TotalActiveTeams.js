import { Card, Typography } from '@mui/material';

const TotalActiveProjects = () => {
    return (
        <Card sx={{ p: 3 }}>
            <Typography variant='subtitle2'>Total Active Teams</Typography>
            <Typography variant='h4'>696</Typography>
        </Card>
    );
};

export default TotalActiveProjects;
